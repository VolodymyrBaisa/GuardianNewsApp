<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ThumbnailStyle">
        <item name="android:scaleType">centerCrop</item>
    </style>


    <style name="SectionNameStyle">
        <item name="android:layout_margin">@dimen/margin</item>
        <item name="android:ellipsize">end</item>
        <item name="android:ems">9</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="PublicationDateStyle">
        <item name="android:layout_margin">@dimen/margin</item>
    </style>

    <style name="TitleStyle">
        <item name="android:layout_marginStart">@dimen/margin</item>
        <item name="android:layout_marginEnd">@dimen/margin</item>
        <item name="android:maxLines">4</item>
        <item name="android:textColor">@color/colorPrimaryText</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TagsContributorStyle">
        <item name="android:layout_margin">@dimen/margin</item>
        <item name="android:maxLines">1</item>
        <item name=" android:gravity">end</item>
    </style>

    <style name="NavDrawerTextStyle">
        <item name="android:textColor">@color/colorNavigationText</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
